<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <script src="https://kit.fontawesome.com/9a6e5a9205.js" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="/stylesheets/style.css">
    </head>
    <body>

        <header>
            <nav class="navbar">
                <div class="brand-title">BIKE<img class="logo" src="images/bike-logo.svg" height="80px">SHOP</div>
            
            <i class="fas fa-bars"></i>
                <div class="nav-links">
                    <ul>
                        <li class="home"><a class="nav-link" href="/">Accueil</a></li>
                        <li class="about"><a class="nav-link" href="#">A propos</a></li>
                        <li class="cart"><a class="nav-link" href="#">Panier</a></li>
                    </ul>  
                </div>
            </nav>
        </header>
    <div class="row">
        <table class="cart-grid">
            <thead class="cart-head">
                <th scope="col">#</th>
                <th scope="col">Image</th>
                <th scope="col">Modèle</th>
                <th scope="col">Quantité</th>
                <th scope="col">Prix</th>
                <th scope="col">Total</th>
                <th scope="col">Supprimer</th>
            </thead>
            <tbody>
                <tr class="cart-items">

                    <%
                    for(var i=0; i<dataCardBike.length; i++) {
                    %>
                    
                    <td><%=dataCardBike[i].position%></td>
                    <td><img src=<%=dataCardBike[i].url%> class="cart-img"></td>
                    <td><%=dataCardBike[i].name%></td>
                    <td><input type="text" value="<%=dataCardBike[i].quantity%>" name="quantity" class="input-cart"><button class="button-quantity"><i class="fas fa-sync-alt"></i></button></td>
                    <td><%=dataCardBike[i].price%> €</td>
                    <td><%=dataCardBike[i].quantity * dataCardBike[i].price%> €</td>
                    <td><button class="button-suppr"><i class="fas fa-trash-alt"></i></button></td>
                </tr>
            </tbody>
            <%
            }
            %>
        </table>
    </div>

        <%
        var totalCart = 0;
        for(var i=0; i<dataCardBike.length; i++) {
        totalCart = (dataCardBike[i].quantity * dataCardBike[i].price) + totalCart;
        }
        %>
    <div>
        <h3 class="total">Total Panier : <%=totalCart%> €</h3>
    </div>
       
        <script src="/javascripts/script.js"></script>
    </body>
</html>